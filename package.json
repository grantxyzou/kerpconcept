{
  "name": "kerp-concept",
  "private": true,
  "description": "Mobile-first redesign proof of concept for kerp.ca",
  "scripts": {
    "build": "node build.js",
    "check": "node build.js && node scripts/shots.js",
    "states": "node build.js && node scripts/states.js",
    "serve": "python3 -m http.server 8000"
  },
  "devDependencies": {
    "playwright": "^1.62.1"
  }
}
