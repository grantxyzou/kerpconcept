{
  "name": "kerp-concept",
  "private": true,
  "description": "Mobile-first redesign proof of concept for kerp.ca",
  "scripts": {
    "build": "node build.js",
    "check": "node build.js && node scripts/shots.js && node scripts/a11y.js",
    "states": "node build.js && node scripts/states.js",
    "serve": "python3 -m http.server 8000",
    "a11y": "node build.js && node scripts/a11y.js"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.13.0",
    "axe-core": "^4.13.0",
    "playwright": "^1.62.1"
  }
}
